stages:
  - test

test-backend:
  stage: test
  tags:                 # <--- 加上这个！
    - SH-Drug-Mgmt    # <--- 填上你 Runner 的“标签”（你创建时填的那个）
  
  # (下面的 image, script 不变)
  image: python:3.10
  script:
    - cd backend
    - pip install -r requirements.txt
    - pip install pytest pytest-flask pytest-mock
    - pytest

test-frontend:
  stage: test
  tags:                 # <--- 加上这个！
    - SH-Drug-Mgmt    # <--- 两个任务都要加！
        
  image: node:18
  script:
    - npm install
    - npm run test