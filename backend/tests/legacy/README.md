# legacy 测试用例

这些测试文件是从 `backend/` 根目录收拢迁移而来，主要用于历史回归与临时验证。

注意事项：
- 优先在 `backend/tests/` 目录内新增或维护正式的单元/集成测试。
- 如需长期保留，请逐步将可复用的断言/场景抽取到标准测试中，并删除重复或失效的用例。
- 本目录中的测试将继续被 pytest 收集执行（路径匹配 `tests/**/test_*.py`）。
